{
  "name": "plant-tracker",
  "version": "1.0.0",
  "description": "Plant Tracker is a lightweight PHP application for managing your houseplants. It stores plant records in a MySQL database and provides a simple web interface to track watering and fertilizing. The project also includes small calculators for estimating water requirements using local weather data.",
  "main": "script.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --experimental-vm-modules ./node_modules/.bin/jest",
    "build:css": "npx tailwindcss -c tailwind.config.js -o css/tailwind.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "jest": "^30.0.3",
    "jest-environment-jsdom": "^30.0.2",
    "tailwindcss": "^3.3.5",
    "@tailwindcss/forms": "^0.5.3"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
